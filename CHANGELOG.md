# 版本记录

## 0.1.5 · 2026-09-16

### 可选丢帧与必要修复

- **新增设置**：压缩区增加 Drop frames / 丢帧下拉选择，默认关闭。支持移除连续相同帧、每 2 / 3 / 4 帧移除第 2 / 3 / 4 帧。中英文文案使用现有 Chrome i18n 资源；设置贯通 UI、偏好、队列和历史重新编辑。旧设置及旧任务补齐 `none`，不重置其他参数。
- **保留原始时钟**：先按倍速采样、限制尺寸，再在 FFmpeg 工作线程分析完整 RGBA 摘要，选择保留帧后生成调色板并编码。保留实际 PTS、使用可变帧间隔并显式设置 GIF `final_delay`，使被删除尾帧和全片合并为单帧时也不改变时长。成品检查保留帧数及总时长。相同尺寸/帧率下复用摘要分析，超限自动降帧缩图后重新分析并压缩。无新增运行时依赖。
- **动画边界误采样**：真实颜色序列测试暴露累计浮点秒数使 0.6 秒仍落在上一帧。图像工作线程统一使用 WebCodecs 整数微秒时间轴，仅在接口边界转换秒，避免重复帧分组及停留时长错误。
- **末尾颜色缺失**：周期删除暴露原 `palettegen=stats_mode=diff` 只计入变化前像素，可能遗漏末尾新颜色。统一使用 `stats_mode=full` 统计保留画面，再执行帧差分编码压缩；不靠复制尾帧或取消丢帧掩盖错误。开源实现依据：[FFmpeg palettegen](https://github.com/FFmpeg/FFmpeg/blob/n5.1.4/libavfilter/vf_palettegen.c)；时间轴依据：[FFmpeg GIF muxer](https://ffmpeg.org/ffmpeg-formats.html#gif)、[framehash](https://ffmpeg.org/ffmpeg-formats.html#framehash)。

### 验证

- 类型检查、44 项单元/契约测试、生产构建通过。
- 新增 `npm run test:drop-frames`：真实扩展 UI 生成、旧设置默认关闭、偏好持久化及历史恢复；五种选项的独立逐帧颜色、帧数、每帧延时校验；重复首尾、全段单画面、0.5× / 2×、12 fps 与 1.5× / 2.5×、不足一帧的结尾、透明 WebP 无残影、超限再次降帧缩图压缩。
- 验证记录：`test-results/drop-frames/verification.json`；界面：`test-results/drop-frames/settings-0.1.5.png`。
- 现有 `test:e2e` 与 `test:preferences` 全部通过：14 个既有流程成品独立解码，原倍速、体积限制、透明合成、取消恢复、动画选区、并发队列及旧 12→10 迁移保持有效。

### 尚未完成

- 升级已有安装需要重载扩展并重开侧栏，确认显示 0.1.5。
- 既有范围缺口仍见 [0.1.4 Review](docs/REVIEW-0.1.4.md)：真实登录态 X、Shorts / 跨域 iframe 专项验收、片段预览和缩略图帧条、长片段预算及商店发布准备。本次不扩展这些功能。

### GitHub 发布准备

- 整理中英文 README、安装使用、开发、隐私权限、Release 操作和该版本发布说明；保留历史规划与 Review 的时间边界。
- 安装包加入项目许可证和第三方完整许可文本。锁定 core 0.12.10 的真实上游发布提交（并非名称相似的 v0.12.10 仓库标签），附带 19 个源码归档、构建说明和 SHA-256。
- 打包校验拒绝混入测试站点权限、版本不匹配或缺少许可的构建；发行校验比对编码器字节与源码归档摘要。`.env*`、本地辅助脚本和测试产物排除在 Git 之外。

## 0.1.4 · 2026-09-16

### Review 修复与原因

- **文件名契约**：新生成、历史结果下载、改名保存和 UI 文件名预览统一调用同一函数，采用 UTF-8 Base64url 标题前缀 + 任务创建毫秒时间戳 + UUID。不包含中文或空格，标准 Base64 的 `/`、`+`、`=` 转成文件名安全形式；有界编码防止长标题超出文件系统限制。同毫秒任务以 UUID 区分，重复保存不更改生成时间。下载目录改为 `GifToolkit`。
- **动图选区丢失（P1）**：页面扫描不含 GIF / WebP 动画时长，原先覆盖 UI 解码信息，生成时又无条件回到整段。后台按素材身份保留解码元数据，片段仅在尚未初始化时补齐默认范围；旧手动选区不改动。增加右键动图读取入口，允许生成前选择多个片段。
- **权限请求时机（P2）**：图片批次在点击动作内统一收集并申请必要来源权限，避免先等待其他图片解码再发起下一次权限请求。
- **时长校验缺口（P1）**：单帧成品原先跳过所有时长检查。现在仅真实静态来源豁免，动画即使编码为一帧也必须覆盖所选时长。
- **队列并发接收（P2）**：多个面板可能在异步写入前同时通过容量检查。改为串行接收、整批事务写入后入队，避免突破 24 个任务上限或半批落库。
- **历史回收（P2）**：删除旧产物后原界面仍可能显示下载卡片。回收后主动通知侧栏移除任务及相关预览，避免对不存在的产物继续提供保存按钮。

### 验证

- 类型检查、37 项契约测试、生产构建通过。
- Chromium 148 全链路通过：同视频两段 + 另一视频、默认全时长/10 fps、0.5×/2×、GIF/WebP、透明合成、先压缩后降帧缩图、字节/尺寸/时长检查、取消、恢复播放器、侧栏重开、来源变更拒绝。
- 新增右键动图编辑两段、重新扫描保留选区并按原范围产出；3 个并发批次共 36 项请求，只接收 24 项，剩余批次明确拒绝；已接收任务可分别取消。
- 偏好迁移、同帖命名模拟、UI 改名及 31 条历史回收至 30 条后侧栏同步通过。
- 真实 Chrome 下载落盘的 Base64url / 时间戳文件名通过，文件逐字节与原成品一致，历史帧率保持不变。
- 公开 YouTube 样片本轮复验：Big Buck Bunny，10–11 秒、2× → 480 × 269、5 帧、0.5 秒、336,318 字节。测试使用独立 Chromium 配置，不连接用户账号。

### 尚未完成

- 真实登录态 X 完整流程、Shorts、跨域 iframe 播放器专项验收。
- 原片段独立预览与真实缩略图帧条；超出当前 600 采样帧的长片段处理方案。
- 商店发布材料、图标、第三方许可文本及对应源码分发完善。不能将当前本地测试版称为已全面验收或可直接上架版本。

完整范围和证据见 [Review](docs/REVIEW-0.1.4.md)。

## 0.1.3 · 2026-09-16

### 修复与原因

- **倍速入口**：原输入框混在压缩参数中，难以发现。移至已选片段顶部，提供独立输入框和 0.5× / 1× / 1.5× / 2× / 3× 按钮，并说明影响全部片段、同步显示成品时长。
- **默认帧率未生效**：旧的未版本化设置将 12 fps 默认值存入本地，覆盖代码中的 10 fps。新增后台管理的版本化偏好和一次性迁移；旧值 12 迁移为 10，其他参数保留，此后显式设置 12 不再被重置。旧数据无法区分显式 12 与旧默认值，迁移仅执行一次；历史任务参数不参与迁移。
- **素材命名**：不再把 `aria-label` 当成内容标题。X 从当前媒体所属 article 提取帖子正文，无正文时使用该帖作者和帖子编号；其他页面使用媒体标题、图片说明、figure 标题、文章标题或页面元数据。避免语言黑名单及误用其他帖子的正文。
- **保存命名**：生成前和成品保存前均可编辑 GIF 名称，文件名自动附加片段范围、倍速并过滤文件系统非法字符。保存时更新任务名称和文件名元数据，不重新编码。无法还原的历史帖子名称不猜测覆盖，可由用户编辑。
- 新文案和错误提示均提供中英文资源；源码保留版本注释说明迁移和命名原因。

### 验证与剩余工作

- 类型检查、26 项契约测试、生产构建通过。
- 独立 Chromium 配置下验证旧 12→10 迁移、随后自选 12 持久化、完整默认选区、倍速按钮和名称从界面传入真实任务；3 秒视频 2× 输出 1.5 秒 / 15 帧。
- X 结构模拟页面验证多个帖子分别命名、无正文帖子定位；普通网页验证忽略通用无障碍标签和读取语义标题。此项不等同于真实 X 页面验收。
- Chrome 下载验证中文自定义文件名、历史任务名称持久化、历史帧率保留及 GIF 文件逐字节一致。
- 仍待真实登录态 X 从右键到保存的完整复验，以及 YouTube Shorts / 嵌入播放器专项测试。

## 0.1.2 · 2026-09-16

### 功能与处理顺序

- 默认选区为媒体完整时长，不再从当前播放位置取 5 秒。未知视频时长明确等待加载，不虚构默认片段；图片在解码后使用完整动画时长。
- 默认 10 fps，新增 0.1–10× 速度设置和成品时长提示；旧设置和历史任务按 1× 迁移，保留用户保存过的帧率。
- 先按成品时间进行倍速采样，再等比例限制尺寸（最大 800），然后保持尺寸和帧率进行调色板压缩。仍超限时才降低帧率、缩小尺寸，再重复压缩。
- 每个尺寸层级按 256 / 128 / 64 色优化，从相同原始采样帧重编，避免累积有损处理；不改变完整选区。

### 根因与回归约束

- **倍速与时间基准**：`原片时间 = 选区起点 + 成品帧序号 × 倍速 / 成品帧率`；帧数和资源预算均以调速后时长计算。避免只改 GIF 帧延迟却让帧率一起改变，或仍按原时长误拒绝加速后的任务。
- **完整片段内存**：原 split 调色板图会保留整段 RGBA 流；改为调色板生成、逐帧编码两个顺序 pass，解码工作集和 PNG 总存储分别限额，不提前降尺寸或截短片段。
- **顺序保证**：首次缩尺寸前完成当前尺寸 / 帧率下的全部调色板优化；每个缩减层级重新优化。加入压缩候选顺序和真实超限成品回归。

### 验证

- 类型检查、21 项契约测试和构建通过。
- Chrome 148 端到端：完整默认选区、10 fps、2× UI 和时长提示、关闭重开侧栏通过。
- 3 秒视频：0.5× 输出 6 秒 / 60 帧，2× 输出 1.5 秒 / 15 帧；GIF、WebP、透明 WebP 的 2× 输出及透明合成通过。
- 竖屏 1.5 秒片段限制 80,000 字节：第 15 次优化得到 203 × 361、78,174 字节，覆盖原选区；无法达标的极小预算仍明确失败。
- 独立 GIF 解码、播放器恢复、取消、来源变更拒绝、Chrome 实际下载及字节一致性通过。
- 真实登录态 X 从右键到保存的人工界面流程仍待完成，不能用上述自动化验证替代。

## 0.1.1 · 2026-09-16

- **右键入口根因**：X 的页面事件处理器取消浏览器原生菜单，单独注册 `contextMenus` 无法显示扩展命令。媒体事件改为在 `window` 捕获阶段阻止后续页面处理器，保留默认浏览器菜单；不修改网站脚本或全局 `preventDefault`。
- 已授权站点改为 `document_start` 注入，并迁移旧注册。首次遇到网站菜单时，从工具栏打开扩展、启用当前站点并刷新即可。
- Alt + 右键保留网站菜单。遮罩层根据实际视频边界定位，多个视频不误选；普通文字、链接和播放器按钮不接管。
- 中英文操作说明同步更新。真实 X 验收结果将在完成后记录。

## 0.1.0 · 2026-09-15

### 功能

- 建立独立运行的 Manifest V3 插件，右键 / 工具栏打开侧栏，支持视频、GIF、WebP、多素材和多个时间段。
- 采用网页播放器取帧与浏览器动画解码，FFmpeg WASM 在后台工作线程编码。
- 默认 4,000,000 字节、宽高最多 800 像素；按真实成品检查尺寸、体积及动画时长。
- 增加任务队列、取消、成品预览和下载；中英文从资源文件读取。

### 根因与回归约束

- **4 MB 单位一致**：UI、任务及成品比较均使用十进制字节，避免 4 MiB 被误当成平台 4 MB。
- **长边精度**：按整数目标长边计算另一轴，避免浮点乘法把 480 像素误缩为 479；加入回归样例。
- **播放器身份**：DOM 节点复用并不代表同一视频。开始处理时验证来源 URL、页面 URL 和海报；变更后拒绝旧任务。
- **播放器状态归属**：自定义播放器可能在跳转后自动恢复播放。取帧期间临时持有暂停状态，结束时解除监听并恢复用户状态，防止抓到错误时间点。
- **帧数据传输**：采用 Chrome 148+ 的 structured clone，传送有界 Blob；不把 Blob 交给 JSON 序列化导致数据丢失。
- **解码内存预算**：开始取帧前按帧尺寸、数量与调色板管线缓存估算内存；不能只检查压缩后的 PNG 大小而允许 WASM 堆被撑满。
- **任务生命周期**：转码与侧栏分离，后台重建时把未完成的旧任务标记为中断；取消后不把已写入的临时结果提交为成功。
- **压缩质量**：各候选从同一组源帧编码，不用前一次有损结果继续压缩；超过质量下限仍不达标时返回规格失败。
- **权限与上下文**：普通右键只记录定位信息，只有调用扩展入口时更新工作素材；页面脚本不允许派发后台任意任务。
- **可访问性**：生成按钮的装饰符号不参与可访问名称，保证辅助技术和用户操作指向同一个明确动作。

### 验证记录

- 12 项输出 / 参数 / 本地化契约测试、类型检查、生产构建通过。
- 真实 Chrome 148：多媒体右键定位、横竖视频、GIF、动态 WebP、透明动画无残影、规格失败、取消、侧栏重开、复用节点拒绝均通过。
- 公开 YouTube 样片：10–11 秒 → 480 × 270、8 帧、551,195 字节 GIF；取帧和编码通过。来源：`https://www.youtube.com/watch?v=aqz-KE-bpKQ`。
- Chrome 下载 API 完成保存，下载字节数与已验证 GIF 完全一致。

### 仍待完成

- X 登录态页面实测、YouTube Shorts 与嵌入播放器专项测试。
- 原片段独立预览、缩略图帧条和更多真实动画边界样本。
- 发布前完善图标、商店材料、第三方组件对应源码分发与审核。
